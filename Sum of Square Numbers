class Solution:
    def judgeSquareSum(self, c: int) -> bool:
        for i in range(int(sqrt(c))+1):
            a = c - i * i
            a = sqrt(a)
            if a == int(a):
                return True
        return False
                
        
